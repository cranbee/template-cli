{
  "name": "@cranbee/template-cli",
  "version": "0.4.0",
  "description": "Cranbee Template CLI",
  "main": "index.js",
  "scripts": {
    "test": "node index.js compile test/test.html | cmp test/test.json && echo success",
    "patch-release": "npm version patch && npm publish --access=public && git push --follow-tags",
    "minor-release": "npm version minor && npm publish --access=public && git push --follow-tags",
    "generate-sample": "node index.js compile test/test.html > test/test.json"
  },
  "author": "DMA <dma@cranbee.com>",
  "license": "MIT",
  "bin": {
    "cranbee-template": "./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cranbee/template-cli.git"
  },
  "dependencies": {
    "@cranbee/template": "^0.5.0",
    "commander": "^2.11.0"
  }
}
